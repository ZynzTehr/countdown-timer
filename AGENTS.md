# Skills & Engineering Standards (Matt Pocock Skills Integration)

Skills installed from `https://github.com/mattpocock/skills`:
- Installed into `.agents/skills/` (38 skills including `implement`, `tdd`, `grilling`, `code-review`, `diagnosing-bugs`, `to-spec`, `domain-modeling`, `ask-matt`, etc.)
- Strict engineering discipline:
  1. Inspect the codebase thoroughly before making assumptions.
  2. Implement with high visual and structural craft.
  3. Verify builds (`npm run build`) and perform visual browser testing.
